function handlerSomething() {
    let bird = "pakornpong"
    if (bird === "pakornpong") {
        var pang = "nan"
        let birds = "bird"
    }

    console.log('pang => var', pang)
    console.log('birds => let', birds) // ไม่เจอ
}

handlerSomething()
